programa factoriales;


entero funcion fact(entero var){
    entero acum;
    acum = var;
    mientras ( var > 1) {
        var = var - 1;
        acum = acum * (var);
    }
    regresa acum;
}

principal(){
    escribe(fact(8));
}